[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install flask flask-cors requests gunicorn --break-system-packages"]

[start]
cmd = "python -m gunicorn server:app --bind 0.0.0.0:$PORT"
